import './globals.css';
export const metadata = {
  title: 'GENESIS — The Parent Evolution Program',
  description: 'Your health is their inheritance.',
};
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
